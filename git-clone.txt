Git clone - clones the history of a Git repository into a local working directory where it can thereby be looked at and modified. The process creates remote-tracking branches for each branch in the cloned repository.